class showHideSection {
    constructor(parameters) {
        const hidden = "hidden";
        const section = document.getElementById("description")
        document.getElementById("verSec").addEventListener("click", Section)

        function Section(){
            
        }
    
    
    }
}